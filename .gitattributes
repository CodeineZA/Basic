# LF in the repository, native in the working copy. Explicit rather than relying
# on whatever core.autocrlf happens to be set to on the next machine.
* text=auto eol=lf
*.png binary
*.jpg binary
*.ico binary
